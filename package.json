{
  "name": "staking",
  "version": "1.0.0",
  "description": "",
  "main": "deploy.js",
  "scripts": {
    "test": "npx hardhat run scripts/deploy.js --network localhost && npx hardhat run scripts/minting.js --network localhost && npx hardhat run scripts/staking.js --network localhost && npx hardhat run scripts/withdraw.js --network localhost && npx hardhat run scripts/getRewards.js --network localhost",
    "enterStaking": "npx hardhat run scripts/deploy.js --network localhost && npx hardhat run scripts/minting.js --network localhost && npx hardhat run scripts/staking.js --network localhost",
    "deploy": "npx hardhat run scripts/deploy.js --network localhost",
    "mint": "npx hardhat run scripts/minting.js --network localhost",
    "stake": "npx hardhat run scripts/staking.js --network localhost",
    "withdraw": "npx hardhat run scripts/withdraw.js --network localhost",
    "reward": "npx hardhat run scripts/getRewards.js --network localhost"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/r0ugeEngine/Staking.git"
  },
  "keywords": [],
  "author": "Bohdan Pukhno",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/r0ugeEngine/Staking/issues"
  },
  "homepage": "https://github.com/r0ugeEngine/Staking#readme",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^1.0.2",
    "@openzeppelin/contracts": "^4.7.3",
    "hardhat": "^2.10.2"
  }
}
